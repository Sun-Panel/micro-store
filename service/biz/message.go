package biz

type MessageType struct {
}

const (
	MESSAGE_SEND    = "send"
	MESSAGE_RECEIVE = "receive"
)

// func (m *MessageType) GetList(msgType string) (list []models.Message, err error) {

// 	global.Db.Find(&list,)
// }
